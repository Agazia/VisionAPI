@model CognetiveContainer

@{
    ViewData["Title"] = "Home Page";
}

<div id="uploadsection">
    <div class="alert h6 alert-info shadow-sm text-center">Ein KI-Dienst, der Inhalte in Bildern analysiert und die Texte in Sprache umwandelt</div>
    <fieldset class="card-shadow border p-2">
        <legend class="w-auto">Bild analysieren</legend>
        <div class="px-3 mt-2">
            <form method="post" enctype="multipart/form-data">
                <div class="row mx-auto mb-2">
                    <div class="col-auto col-md-6 mx-auto">
                        <fieldset class="card-shadow border p-2">
                            <legend class="w-auto">Bild hochlagen</legend>
                            <div class="px-3 mt-2">
                                <div class="form-group">
                                    <input class="card p-2 w-100" id="inputpic" type="file" accept="image/*" />
                                    <small>Unterstützte Bilddatei: "jpeg, png, gif", maximale Dateigröße: 4 MB</small>
                                </div>
                            </div>
                        </fieldset>
                    </div>
                    <div class="col-auto col-md-6 mx-auto">
                        <fieldset class="card-shadow border p-2">
                            <legend class="w-auto">Url eingeben</legend>
                            <div class="px-3 mt-2">
                                <div class="form-group">
                                    <input asp-for="ImageAnalysisVM.Url" class="card p-2 w-100" id="inputurl" type="text" />
                                    <small>Bild-URL hier eingeben!</small>
                                </div>
                            </div>
                        </fieldset>
                    </div>
                </div>
                <div class="text-center">
                    <a class="btn btn-outline-primary" href="javascript:analyze_imgAndSpeech();">Bild analysieren</a>
                    @*<a class="btn btn-outline-danger" href="javascript:delete_img('@Model.ImageAnalysisVM.Url');" id="deletepic">Löschen</a>*@
                </div>
            </form>
        </div>
    </fieldset>
</div>

<div id="resultsection" class="analyse">

</div>





<!-- Modal -->
<div class="modal fade" id="loadingModal" tabindex="-1" role="dialog" data-keyboard="false" data-backdrop="static" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content p-3">
            <div id="modal-content" class="text-center">
                <div class="spinner-border" role="status">
                </div>
                <h4>Das Bild wird analysiert</h4>
                <p>Bitte um etwas Geduld!</p>
            </div>
        </div>
    </div>
</div>
